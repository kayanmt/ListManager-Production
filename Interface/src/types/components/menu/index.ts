export declare type MenuProps = {
	path: "/" | "/settings" | "/login";
};
export interface MenuItemButtonProps {
	active?: boolean;
}
export interface MenuItemProps {
	logout?: boolean;
	active?: boolean;
}
